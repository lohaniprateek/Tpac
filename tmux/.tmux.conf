##### BASIC SETTINGS #####
set -g mouse on
set -g status-position top
set -g history-limit 100000
set -g mode-keys vi

##### PANE NAVIGATION #####
bind -n C-h select-pane -L
bind -n C-j select-pane -D
bind -n C-k select-pane -U
bind -n C-l select-pane -R

##### WINDOW LIST FORMATS #####
# Inactive windows
#rset -g window-status-format "#[fg=colour250,bg=colour235] #I:#W#F #[default]"

###### ACTIVE WINDOW — COLOR PER INDEX #####
#set -g window-status-current-format "#[\
#bg=#{?#{==:#{window_index},0},green,\
##{?#{==:#{window_index},1},blue,\
##{?#{==:#{window_index},2},yellow,\
##{?#{==:#{window_index},3},magenta,\
##{?#{==:#{window_index},4},cyan,\
##{?#{==:#{window_index},5},red,\
##{?#{==:#{window_index},6},colour33,\
##{?#{==:#{window_index},7},colour129,colour208}}}}}}}}\
#,fg=#{?#{==:#{window_index},0},black,\
##{?#{==:#{window_index},1},white,\
##{?#{==:#{window_index},2},black,\
##{?#{==:#{window_index},3},black,\
##{?#{==:#{window_index},4},black,\
##{?#{==:#{window_index},5},white,\
##{?#{==:#{window_index},6},white,\
##{?#{==:#{window_index},7},white,black}}}}}}}}\
#,bold] #I:#W#F #[default]"
#
##### QUICK RELOAD #####
bind r source-file ~/.tmux.conf \; display "tmux reloaded!"

# Scrollback / copy-mode
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-selection-and-cancel
bind -n PageUp copy-mode -u
bind -n WheelUpPane if-shell -F "#{pane_in_mode}" "send-keys -M" "copy-mode -e"

if-shell "[ -f ~/.config/tmux/gruvbox.conf ]" "source-file ~/.config/tmux/gruvbox.conf" "source-file ~/Tpac/tmux/gruvbox.conf"
# source ~/Tpac/tmux/theme.conf
# source ~/Tpac/tmux/statusline.conf
